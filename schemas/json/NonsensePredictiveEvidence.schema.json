{
  "additionalProperties": false,
  "description": "The nonsense predictive (PRD) step of a nonsense branch (SM 8).\n\nNMD-predicted (yellow) starts at a fixed +6.0; the rescue (orange) and no-NMD\n(violet) branches derive initial points from the fraction of protein lost.\nPositive points are reduced by the SM 18 mechanism/exon matrix.",
  "properties": {
    "adjusted_points": {
      "anyOf": [
        {
          "type": "number"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Coded PRD points after the SM 18 adjustment.",
      "title": "Adjusted Points"
    },
    "alternative_met_rescue": {
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Evidence an alternative-Met start codon rescues function (orange branch).",
      "title": "Alternative Met Rescue"
    },
    "basis": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Predictive basis (e.g. NMD prediction; PTC position).",
      "title": "Basis"
    },
    "initial_points": {
      "anyOf": [
        {
          "type": "number"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Initial PRD points before the SM 18 adjustment.",
      "title": "Initial Points"
    },
    "protein_fraction_reduced": {
      "anyOf": [
        {
          "type": "number"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Fraction of protein lost (the orange/violet initial-points table).",
      "title": "Protein Fraction Reduced"
    }
  },
  "title": "NonsensePredictiveEvidence",
  "type": "object"
}
