{
  "$defs": {
    "CoOccurrenceLikelihood": {
      "description": "gnomAD co-occurrence likelihood that the VBC and the in-trans variant are\ntwo independent rare heterozygous variants, used to place a biallelic\n``CLN_AFF`` proband in the correct scoring-table row (SVCv4 Supplementary\nMaterial 4). Computed as (in-trans + unphased counts) / gnomAD v2 exome\ntotal; SM 4 buckets it as <0.0001 or >0.0001-0.01.\n\n``NOT_ASSESSED`` means the analyst did not/could not compute it \u2014 distinct\nfrom the field being absent (``None``), which means it was not captured.",
      "enum": [
        "LT_0_0001",
        "BETWEEN_0_0001_0_01",
        "NOT_ASSESSED"
      ],
      "title": "CoOccurrenceLikelihood",
      "type": "string"
    },
    "PhaseConfidence": {
      "description": "Confidence in a phase determination.",
      "enum": [
        "HIGH",
        "MED",
        "LOW"
      ],
      "title": "PhaseConfidence",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "description": "The second variant in a biallelic evaluation against a het VBC.\n\nIncluded only when ``vbc_zygosity`` is ``HET`` and there is another variant\nin the same gene that is also HET and in *trans*. Because those conditions\nare the inclusion criteria, zygosity (HET) and phase (TRANS) are implied and\nare not captured here.",
  "properties": {
    "classification": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Variant classification (placeholder string this phase).",
      "title": "Classification"
    },
    "co_occurrence_likelihood": {
      "anyOf": [
        {
          "$ref": "#/$defs/CoOccurrenceLikelihood"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "gnomAD co-occurrence likelihood bucket for the VBC + in-trans variant pairing (biallelic CLN_AFF, SM 4 Table 2). Captured; row-selection/scoring is documented, not computed."
    },
    "id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Id"
    },
    "phase_confidence": {
      "anyOf": [
        {
          "$ref": "#/$defs/PhaseConfidence"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Confidence that the variant is in trans with the VBC."
    }
  },
  "title": "CompoundHetVariant",
  "type": "object"
}
